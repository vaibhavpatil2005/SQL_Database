## Aggregate Functions :

    # DEfination :

        - Aggregate functions perform a calculation on a se tof values  and return a single value.

        COUNT()
        MAX()
        MIN()
        SUM()
        AVG()


    # Ex

        SELECT max (marks) FROM stuentd


        SELECT AVG(marks) FROM student